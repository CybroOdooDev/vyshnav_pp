<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <record id="product_template_only_form_view" model="ir.ui.view">
        <field name="name">product.template.only.form.inherit.intervlag.attribute.restriction</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='variants']" position="after">
                <page string="Attribute Values Validation">
                    <field name="product_attribute_rule_ids">
                        <tree editable="bottom">
                            <field name="attribute_id"/>
                            <field name="values_ids" widget="many2many_tags"/>
                            <field name="restriction_config_id"/>
                        </tree>
                    </field>
                    <group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
    </data>
</odoo>
